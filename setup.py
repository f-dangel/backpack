from setuptools import setup

setup(name='backprop-extended',
      version='0.1',
      description='hidden',
      url='hidden',
      author='hidden',
      author_email='hidden',
      license='',
      packages=['bpexts'],
      zip_safe=False
      )
